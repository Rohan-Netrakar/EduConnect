<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Classroom Engagement System</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    :root {
      --primary: #4361ee;
      --primary-light: #4895ef;
      --secondary: #3f37c9;
      --accent: #4cc9f0;
      --dark: #1d3557;
      --light: #f8f9fa;
      --success: #2ecc71;
      --shadow: 0 10px 30px rgba(0,0,0,0.08);
      --radius: 16px;
      --transition: all 0.3s ease;
    }

    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #e4edf5 100%);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      color: var(--dark);
      line-height: 1.6;
      padding: 20px;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      width: 100%;
    }

    /* Header Styles */
    header {
      padding: 20px 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .logo i {
      font-size: 2.5rem;
      color: var(--primary);
    }

    .logo-text {
      font-size: 1.8rem;
      font-weight: 700;
      background: linear-gradient(to right, var(--primary), var(--secondary));
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }

    nav ul {
      display: flex;
      list-style: none;
      gap: 30px;
    }

    nav a {
      text-decoration: none;
      color: var(--dark);
      font-weight: 500;
      font-size: 1.1rem;
      transition: var(--transition);
      padding: 8px 12px;
      border-radius: var(--radius);
    }

    nav a:hover {
      color: var(--primary);
      background: rgba(67, 97, 238, 0.1);
    }

    /* Hero Section */
    .hero {
      display: flex;
      align-items: center;
      padding: 80px 0;
      gap: 50px;
    }

    .hero-content {
      flex: 1;
    }

    .hero h1 {
      font-size: 3.5rem;
      line-height: 1.2;
      margin-bottom: 20px;
      background: linear-gradient(to right, var(--dark), var(--secondary));
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }

    .hero p {
      font-size: 1.3rem;
      color: #4a5568;
      max-width: 600px;
      margin-bottom: 40px;
    }

    .cta-button {
      display: inline-block;
      background: linear-gradient(to right, var(--primary), var(--primary-light));
      color: white;
      padding: 18px 36px;
      font-size: 1.2rem;
      font-weight: 600;
      border-radius: var(--radius);
      text-decoration: none;
      transition: var(--transition);
      box-shadow: var(--shadow);
      border: none;
      cursor: pointer;
    }

    .cta-button:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 25px rgba(67, 97, 238, 0.4);
    }

    .hero-image {
      flex: 1;
      display: flex;
      justify-content: center;
    }

    .hero-img-container {
      width: 100%;
      max-width: 500px;
      height: 400px;
      background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
      border-radius: var(--radius);
      position: relative;
      overflow: hidden;
      box-shadow: var(--shadow);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .hero-img-container::before {
      content: "";
      position: absolute;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0) 70%);
      transform: rotate(30deg);
    }

    .hero-img-content {
      text-align: center;
      color: white;
      padding: 30px;
      z-index: 2;
    }

    .hero-img-content i {
      font-size: 8rem;
      margin-bottom: 20px;
      opacity: 0.9;
    }

    .hero-img-content h2 {
      font-size: 2.2rem;
      margin-bottom: 15px;
    }

    /* Features Section */
    .features {
      padding: 80px 0;
      text-align: center;
    }

    .section-title {
      font-size: 2.5rem;
      margin-bottom: 15px;
      color: var(--dark);
    }

    .section-subtitle {
      font-size: 1.2rem;
      color: #718096;
      max-width: 700px;
      margin: 0 auto 60px;
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 30px;
      margin-top: 40px;
    }

    .feature-card {
      background: white;
      border-radius: var(--radius);
      padding: 40px 30px;
      text-align: center;
      box-shadow: var(--shadow);
      transition: var(--transition);
    }

    .feature-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 15px 35px rgba(0,0,0,0.1);
    }

    .feature-icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, var(--accent) 0%, var(--primary-light) 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 25px;
    }

    .feature-icon i {
      font-size: 2.5rem;
      color: white;
    }

    .feature-card h3 {
      font-size: 1.5rem;
      margin-bottom: 15px;
      color: var(--dark);
    }

    .feature-card p {
      color: #4a5568;
    }

    /* Footer */
    footer {
      background: var(--dark);
      color: white;
      padding: 40px 0 20px;
      margin-top: 80px;
      border-radius: var(--radius) var(--radius) 0 0;
    }

    .footer-content {
      display: flex;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 40px;
      margin-bottom: 40px;
    }

    .footer-section {
      flex: 1;
      min-width: 250px;
    }

    .footer-section h3 {
      font-size: 1.5rem;
      margin-bottom: 20px;
      color: var(--accent);
    }

    .footer-section p {
      color: #cbd5e0;
      margin-bottom: 15px;
    }

    .social-links {
      display: flex;
      gap: 15px;
      margin-top: 20px;
    }

    .social-links a {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 40px;
      height: 40px;
      background: rgba(255,255,255,0.1);
      border-radius: 50%;
      color: white;
      transition: var(--transition);
    }

    .social-links a:hover {
      background: var(--primary);
      transform: translateY(-3px);
    }

    .copyright {
      text-align: center;
      padding-top: 20px;
      border-top: 1px solid rgba(255,255,255,0.1);
      color: #a0aec0;
      font-size: 0.9rem;
    }

    /* Responsive Design */
    @media (max-width: 900px) {
      .hero {
        flex-direction: column;
        text-align: center;
        padding: 60px 0;
      }
      
      .hero-content {
        display: flex;
        flex-direction: column;
        align-items: center;
      }
      
      .hero h1 {
        font-size: 2.8rem;
      }
      
      .hero p {
        font-size: 1.1rem;
      }
      
      .hero-img-container {
        height: 350px;
      }
    }

    @media (max-width: 768px) {
      header {
        flex-direction: column;
        gap: 20px;
      }
      
      nav ul {
        gap: 15px;
      }
      
      .hero h1 {
        font-size: 2.3rem;
      }
      
      .section-title {
        font-size: 2rem;
      }
    }

    @media (max-width: 480px) {
      nav ul {
        flex-wrap: wrap;
        justify-content: center;
      }
      
      .hero h1 {
        font-size: 2rem;
      }
      
      .hero-img-container {
        height: 280px;
      }
      
      .hero-img-content i {
        font-size: 6rem;
      }
      
      .hero-img-content h2 {
        font-size: 1.8rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="logo">
        <i class="fas fa-chalkboard-teacher"></i>
        <div class="logo-text">EduConnect</div>
      </div>
      <nav>
        <ul>
          <li><a href="#"><i class="fas fa-home"></i> Home</a></li>
          <li><a href="#"><i class="fas fa-info-circle"></i> About</a></li>
          <li><a href="#"><i class="fas fa-envelope"></i> Contact</a></li>
        </ul>
      </nav>
    </header>

    <section class="hero">
      <div class="hero-content">
        <h1>Transform Your Classroom Experience</h1>
        <p>Engage students with interactive tools, real-time feedback, and collaborative learning environments. Create dynamic classes in seconds.</p>
        <a href="/create-class" class="cta-button">
          <i class="fas fa-plus-circle"></i> Create New Class
        </a>
      </div>
      <div class="hero-image">
        <div class="hero-img-container">
          <div class="hero-img-content">
            <i class="fas fa-users"></i>
            <h2>Interactive Learning</h2>
            <p>Join thousands of educators enhancing their classrooms</p>
          </div>
        </div>
      </div>
    </section>

    <section class="features">
      <h2 class="section-title">Powerful Classroom Features</h2>
      <p class="section-subtitle">Everything you need to create engaging and interactive learning experiences</p>
      
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-poll"></i>
          </div>
          <h3>Live Polling</h3>
          <p>Get instant feedback from students with real-time polls and quizzes during lessons.</p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-hand-paper"></i>
          </div>
          <h3>Virtual Hand Raise</h3>
          <p>Students can virtually raise hands to ask questions without interrupting the flow.</p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-chart-bar"></i>
          </div>
          <h3>Analytics Dashboard</h3>
          <p>Track student participation and engagement with detailed analytics.</p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-comments"></i>
          </div>
          <h3>Q&A Forum</h3>
          <p>Interactive question and answer sessions for collaborative problem solving.</p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-tasks"></i>
          </div>
          <h3>Assignment Tracking</h3>
          <p>Create, distribute, and grade assignments all in one platform.</p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-qrcode"></i>
          </div>
          <h3>Easy Class Access</h3>
          <p>Students join classes instantly with QR codes or class links.</p>
        </div>
      </div>
    </section>
  </div>
  
  <footer>
    <div class="container">
      <div class="footer-content">
        <div class="footer-section">
          <h3>About EduConnect</h3>
          <p>We're revolutionizing classroom engagement with cutting-edge technology designed for educators and students.</p>
          <div class="social-links">
            <a href="#"><i class="fab fa-facebook-f"></i></a>
            <a href="#"><i class="fab fa-twitter"></i></a>
            <a href="#"><i class="fab fa-instagram"></i></a>
            <a href="#"><i class="fab fa-linkedin-in"></i></a>
          </div>
        </div>
        
        <div class="footer-section">
          <h3>Quick Links</h3>
          <p><a href="#" style="color: #cbd5e0; text-decoration: none;">Home</a></p>
          <p><a href="#" style="color: #cbd5e0; text-decoration: none;">Create Class</a></p>
          <p><a href="#" style="color: #cbd5e0; text-decoration: none;">Features</a></p>
          <p><a href="#" style="color: #cbd5e0; text-decoration: none;">Pricing</a></p>
        </div>
        
        <div class="footer-section">
          <h3>Contact Us</h3>
          <p><i class="fas fa-envelope"></i> support@educonnect.com</p>
          <p><i class="fas fa-phone"></i> +1 (555) 123-4567</p>
          <p><i class="fas fa-map-marker-alt"></i> 123 Education St, Learnville</p>
        </div>
      </div>
      
      <div class="copyright">
        <p>&copy; 2025 EduConnect Classroom Engagement System. All rights reserved.</p>
      </div>
    </div>
  </footer>
</body>
</html>